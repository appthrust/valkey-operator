apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: valkey-operator-system
namePrefix: valkey-operator-

resources:
- ../rbac
- ../manager
- ../default/metrics_service.yaml

patches:
- path: manager_metrics_patch.yaml
  target:
    kind: Deployment
